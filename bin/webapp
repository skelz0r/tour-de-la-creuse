#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/.."

PORT="${1:-8080}"

echo "Webapp: http://localhost:$PORT/webapp/"
open "http://localhost:$PORT/webapp/" 2>/dev/null || true
python3 -m http.server "$PORT"
